# Intake 2.4.7 What's New Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Add localized Intake 2.4.7 release notes covering the story-based Intake AI onboarding and performance fixes.

**Architecture:** Add English and German Markdown entries under the existing versioned content directory and one shared SVG cover. The existing content loader, sitemap generator, and prerender pipeline will discover the new version automatically.

**Tech Stack:** Markdown frontmatter, SVG, TypeScript, Vitest, Vite prerendering.

---

### Task 1: Define the new release behavior

**Files:**
- Modify: `src/lib/whatsNewContent.test.ts`

1. Add failing assertions that 2.4.7 is newest in both locales.
2. Assert the publication date, localized summaries, onboarding explanation, subscription/BYOK distinction, and performance section.
3. Run `npm test -- src/lib/whatsNewContent.test.ts` and confirm failure because 2.4.7 does not exist.

### Task 2: Add localized content and cover

**Files:**
- Create: `content/whats-new/2.4.7/en.md`
- Create: `content/whats-new/2.4.7/de.md`
- Create: `content/whats-new/2.4.7/assets/cover.svg`

1. Add English and German frontmatter, highlights, and body copy.
2. Add the shared versioned cover.
3. Run the focused content test and confirm it passes.

### Task 3: Verify static delivery

**Files:**
- Modify only if automatic discovery fails.

1. Run sitemap and prerender tests.
2. Run the full test suite.
3. Run ESLint on touched source files.
4. Run the production build.
5. Run `git diff --check`.
