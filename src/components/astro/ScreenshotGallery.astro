---
import { getScreenshotGalleryAssets } from '@/lib/screenshotAssets';
import { useT } from '@/i18n/t';
import type { Language } from '@/i18n/translations';

/**
 * Was an embla carousel — 23 KB of JS to scroll a row of screenshots sideways. CSS
 * scroll-snap does the same natively: touch, trackpad and shift+wheel all work, and it
 * degrades to a plain scrollable row if scroll-snap is unsupported.
 *
 * The embla prev/next arrows are gone, so the scrollbar is deliberately left visible
 * (styled thin) rather than hidden: without arrows it is the only affordance telling a
 * desktop user the row scrolls at all.
 */
interface Props {
  lang: Language;
}

const { lang } = Astro.props;
const t = useT(lang);
const screenshots = getScreenshotGalleryAssets(lang);
---
<section class="py-16 md:py-20">
  <div class="container mx-auto px-0 md:px-4">
    <h2 class="mb-10 px-4 text-center text-2xl text-foreground md:px-0 md:text-3xl">
      {t('screenshotGalleryHeading')}
    </h2>

    <div class="reveal relative px-0 md:px-4">
      <ul
        class="flex snap-x snap-mandatory gap-4 overflow-x-auto scroll-smooth px-4 pb-4 md:px-0 [scrollbar-color:hsl(var(--muted-foreground)/0.35)_transparent] [scrollbar-width:thin]"
      >
        {screenshots.map((screenshot) => (
          <li class="shrink-0 basis-[85%] snap-center sm:basis-1/2 md:basis-1/3 md:snap-start lg:basis-1/4">
            <figure>
              <img
                src={screenshot.src}
                alt={screenshot.alt}
                loading="lazy"
                decoding="async"
                class="h-auto w-full rounded-xl border border-white/[0.08]"
              />
              <figcaption class="mt-3 px-2 text-sm text-foreground/50">
                {screenshot.caption}
              </figcaption>
            </figure>
          </li>
        ))}
      </ul>
    </div>
  </div>
</section>
