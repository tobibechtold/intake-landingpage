---
import Header from '@/components/astro/Header.astro';
import Footer from '@/components/astro/Footer.astro';
import { pageProps } from '@/lib/pageSeo';
import type { Language } from '@/i18n/translations';

/**
 * Header + footer chrome shared by every page.
 *
 * The header is pure HTML: its mobile menu is a native <dialog>, so no page needs to
 * hydrate anything for the chrome. Page bodies beneath it are static renders too.
 *
 * No <main> is rendered here: page bodies supply their own, and nesting them would be
 * invalid HTML.
 */
interface Props {
  route: string;
}

const { route } = Astro.props;
const { lang, alternateHref } = pageProps(route);
---
<div class="flex min-h-screen flex-col">
  <Header lang={lang as Language} alternateHref={alternateHref} />
  <slot />
  <Footer lang={lang as Language} />
</div>
