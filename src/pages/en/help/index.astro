---
import BaseLayout from '@/layouts/BaseLayout.astro';
import { pageProps } from '@/lib/pageSeo';
import HelpPage from '@/pages-react/HelpPage';

const route = '/en/help';
---
<BaseLayout route={route}>
  <HelpPage client:load {...pageProps(route)} />
</BaseLayout>
