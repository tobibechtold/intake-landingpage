---
import BaseLayout from '@/layouts/BaseLayout.astro';
import { pageProps } from '@/lib/pageSeo';
import ByokGuidePage from '@/pages-react/ByokGuidePage';

const route = '/en/help/own-api-key';
---
<BaseLayout route={route}>
  <ByokGuidePage {...pageProps(route)} />
</BaseLayout>
